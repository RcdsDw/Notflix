import { get } from './api'

async function apiGet(email, password) {
    /* let res = await get(`${process.env.REACT_APP_API}/register/`, { email, password })
    if (res.ok) {
        console.log(res)
        return await res.json()
    } else {
        return false;
    } */
}

export default apiGet