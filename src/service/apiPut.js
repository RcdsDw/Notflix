import { put } from './api'

async function apiPut(email, password) {
    /* let res = await put(`${process.env.REACT_APP_API}/register/`, { email, password })
    if (res.ok) {
        console.log(res)
        return await res.json()
    } else {
        return false;
    } */
}

export default apiPut