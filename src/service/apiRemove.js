import { remove } from './api'

async function apiRemove(email, password) {
    /* let res = await remove(`${process.env.REACT_APP_API}/register/`, { email, password })
    if (res.ok) {
        console.log(res)
        return await res.json()
    } else {
        return false;
    } */
}

export default apiRemove